env: "local" # debug, local, test, production
format_time: "RFC3339"
log_file_path: "app.log"
temp_object_path: "volume"
jwt_secret: "xui"

httpserver:
  port: 8080
  host: localhost

client:
  database:
    host: db_service
    port: 2021

  ml:
    host: ml
    port: 551